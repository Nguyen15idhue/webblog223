# Laragon: Auto-detect PHP version, enforce virtual host domain name
Auto .php

# Use auto generated host name as SERVER_NAME variable
RewriteEngine On
RewriteCond %{HTTP_HOST} ^(.*)\.test$ [NC]
RewriteRule ^ - [E=SERVER_NAME:%1]
